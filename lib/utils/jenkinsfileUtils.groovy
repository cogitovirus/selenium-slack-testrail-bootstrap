/**
*   Verifies that pass threshold has been reached, then generates run summary which is then send to slack
*   @parameter {boolean} success - if build was successful
*   @parameter {string} customMsg - for some additional info
*/
def slackNotify(success, customMsg=""){
    // TODO implement
}